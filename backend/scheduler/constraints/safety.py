"""TODO: maintenance closures, buffers, opposite-bound and interchange conflicts."""

IMPLEMENTED = False


def add_constraints(model, variables, instance, footprints, policy):
    raise NotImplementedError("Safety constraints are not implemented.")
