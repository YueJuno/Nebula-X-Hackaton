"""TODO: workload conservation, earliest starts, activity frequency, predecessors."""

IMPLEMENTED = False


def add_constraints(model, variables, instance, footprints, policy):
    raise NotImplementedError("Workload constraints are not implemented.")
